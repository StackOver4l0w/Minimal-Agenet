# Build artifacts
*.exe
*.o
*.obj

# Local cross-compile toolchain for the .vscode build tasks
.tools/

# Editor / agent local state
.claude/
.agent-docs/
.crossnote/
assets/

# Personal study notes (local only, not part of the project)
notes/

# Local test harness (kept personal for now)
shell_harness.py

# Local-only test oracles (kept out of the repository by choice)
.local-tests/
